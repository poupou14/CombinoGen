CombinoGen :
------------

- install pyhton 2.7
- install pip
	$> sudo apt-get install python-pip
- install pyside
#	$> sudo apt-get install pyside-tools 
install pyside-tools : https://pyside.readthedocs.io/en/latest/building/linux.html
- install cython
	$> sudo apt-get install cython
- install qt4*
	$> sudo apt-get install qt4-*
 install pyExcelerator 
	$> sudo apt-get install python-excelerator 
	$> sudo apt-get install python-xlwt
	$> sudo apt-get install python-xlrd
- get WinaScan, and put it at the same level as CombinoGen
- get Selenium :
	$> pip install selenium
- get xvfb :
	$> sudo apt-get install xvfb
	$> sudo pip install setuptools
	$> sudo pip install pyvirtualdisplay
- get geckodriver : https://github.com/mozilla/geckodriver/releases
	put it in a local directory, and update CombinoGUI.sh : export PATH=...	
- get splinter :
	$> sudo pip install splinter

